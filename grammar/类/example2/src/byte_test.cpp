#include <iostream>
#include <cstddef>
#include <cstring>
 
int main()
{
//    std::string s = "C++";
//
//    std::byte bytes[s.length()];
//    std::memcpy(bytes, s.data(), s.length());
//
//    for (auto &b: bytes) {
//        std::cout << std::to_integer<int>(b) << ' ';
//    }
 
    return 0;
}